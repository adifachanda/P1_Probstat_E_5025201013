x = 3

#Nomor 5a
dexp(x, rate=1,log=FALSE)

#Nomor 5b
hist(rexp(10))
hist(rexp(100))
hist(rexp(1000))
hist(rexp(10000))

#Nomor 5c
n = 100
mean = 1 / x
mean
var = ((mean) * (1 / sqrt(n))) * ((mean) * (1 / sqrt(n)))
var
