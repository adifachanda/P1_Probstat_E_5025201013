x = 3

#Nomor 5a
rexp(x, rate=1)

#Nomor 5b
hist(rexp(10))
hist(rexp(100))
hist(rexp(1000))
hist(rexp(10000))

#Nomor 5c
n = 100
mean = 1 / x
mean
var = ((mean) * (1 / sqrt(n))) * ((mean) * (1 / sqrt(n)))
var
